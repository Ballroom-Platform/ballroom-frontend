import React, { useEffect } from "react";
import { useHistory } from "react-router";
import { Layout } from "../components/templates";

export const FallbackHandler : React.FC = () => {
    return <><Layout></Layout></>
}